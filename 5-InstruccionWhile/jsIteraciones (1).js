function Mostrar()
{
	var contador=0;
	alert('iteración while');
  while (contador<10) 
{
   document.write("6" + contador +"<BR>");
   contador ++;
   console.log(contador);


}
//3 paso dentro del While. 1 Var contador=0; Iniciar el contador. 2 paso cambiar el contador, contador ++; siempre adentro del While.
//3 que la condicion del contador se justa, ej: menos 10, asi pones el limite del enunciado. 

}//FIN DE LA FUNCIÓN