#include <stdio.h>
#include <stdlib.h>

typedef struct
{
    int legajo;
    char nombre[20];
    char apellido[30];

};
int main ()
{
    int i;
    for (i=0; i<30; i++)

    struct alumno;
    printf("Ingrese nombre: ");
    fflush(stdin);
    gets(alumno.nombre);
    printf("Ingrese apellido: ");
    fflush(stdin);
    scanf("%s", alumno.apellido);
    printf("Ingrese promedio: ");
    scanf("%f", &alumno.promedio);
    printf("Ingrese un legajo: ");
    scanf ("%d", &alumno.legajo);
    strcpy(alumno.nombre, alumno.apellido, alumno.promedio, alumno.legajo);



    return (0);
}

